import json
import discord
import json
from discord import embeds

class CoinBankPortal:
    def change_coin_count(self, target_user, amount):
        print(f"adding {amount} to {target_user}'s ChodeCoin count.")
        # Get User Value
        # Add amount to User Value
        # Set User's Value to new Value

    def create_new_user(self, target_user):
        print(f"creating user {target_user} in the database")